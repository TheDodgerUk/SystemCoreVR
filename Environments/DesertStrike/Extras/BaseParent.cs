using UnityEngine;
using System.Collections;

public class BaseParent : MonoBehaviour
{
    // nothing 
    // just used in conjuntion with 
    // MyGlobals.GetBaseParentObject(this.gameObject).transform.position);
}
